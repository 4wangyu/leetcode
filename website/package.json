{
  "scripts": {
    "download": "leetcode-site download && yarn generate-docs",
    "generate-docs": "node ./scripts/generateDocs.js",
    "clear": "node ./scripts/clear.js",
    "start": "docusaurus-start",
    "build": "docusaurus-build",
    "publish-gh-pages": "GIT_USER=wangyu CURRENT_BRANCH=master USE_SSH=true docusaurus-publish",
    "write-translations": "docusaurus-write-translations",
    "version": "docusaurus-version",
    "rename-version": "docusaurus-rename-version"
  },
  "devDependencies": {
    "docusaurus": "^1.7.3",
    "leetcode-site-generator": "^0.2.0"
  }
}
